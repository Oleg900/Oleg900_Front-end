import React from "react";

export default function UserMenu() {

    return (
        <div className="user_menu">
           <a href="#">Profile</a>
           <a href="#">My tasks</a>
           <a href="#">Log Out</a>
        </div>
    );

}